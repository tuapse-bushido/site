##############################################################
# 🌐 PUBLIC CONFIGURATION
# Эти переменные могут быть доступны на клиенте (NEXT_PUBLIC_*).
# Не храни здесь чувствительные данные!
##############################################################

# Домен с изображениями (CDN или S3 публичная зона)
NEXT_PUBLIC_IMAGES_DOMAIN=https://<your-storage>.selstorage.ru
NEXT_PUBLIC_IMAGES_CATEGORIES=$NEXT_PUBLIC_IMAGES_DOMAIN}/categories/

##############################################################
# 🗄️ DATABASE CONFIGURATION
# Используется для локальной и серверной работы.
##############################################################

DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=bushido
DB_USER=<db_user>
DB_PASSWORD=<db_password>

# Полный URL (используется, например, Prisma, Drizzle или Knex)
DATABASE_URL=postgresql://$DB_USER:$DB_PASSWORD@$DB_HOST:$DB_PORT/$DB_DATABASE

##############################################################
# ☁️ CLOUD STORAGE (S3-Compatible)
# Никогда не публикуй реальные ключи!
##############################################################

ACCESS_KEY_ID=<your_access_key>
SECRET_KEY_ID=<your_secret_key>
ENDPOINT=https://s3.storage.selcloud.ru
BUCKET=<your_bucket_name>

##############################################################
# 🔐 JWT AUTHENTICATION
# Используется для генерации токенов (access/refresh).
##############################################################

JWT_SECRET=<your_jwt_access_secret>
REFRESH_SECRET=<your_jwt_refresh_secret>

##############################################################
# 📱 SMS PROVIDERS
# Используй только для серверных функций (не в NEXT_PUBLIC_*).
##############################################################

# --- SMS Aero ---
# Формат запроса (в примере логин и пароль заменены переменными)
SMS_AERO_LOGIN=<your_login>
SMS_AERO_PASSWORD=<your_password>
NEXT_PUBLIC_SMS_SEND=https://$SMS_AERO_LOGIN:$SMS_AERO_PASSWORD@gate.smsaero.ru/v2/sms/send

# --- SMS Pilot ---
SMS_PILOT_API_KEY=<your_api_key>
SMS_PILOT_SEND=https://smspilot.ru/api.php?apikey=$SMS_PILOT_API_KEY&format=json&

##############################################################
# ⚙️ ENVIRONMENT SETTINGS
##############################################################

NODE_ENV=development
